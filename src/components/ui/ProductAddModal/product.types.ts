export type TProduct = {
  model: string;
  battery: string;
  ram: string;
  storage: string;
  screenSize: string;
  camera: string;
  price: number | string;
  quantity: number;
  brand: string;
  operatingSystem?: string;
  releaseDate?: string;
};
